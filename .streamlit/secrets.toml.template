# ----------------------------------------------------------------
# Streamlit Cloud / 로컬 실행용 시크릿 템플릿
# ----------------------------------------------------------------
# 사용법:
#  1) 로컬: 이 파일을 secrets.toml로 복사하고 값을 채운다.
#     (secrets.toml은 .gitignore에 등록되어 커밋되지 않음)
#  2) Streamlit Community Cloud:
#     앱 설정 → Secrets에 동일 키 = 값 형식으로 직접 입력.
#     이 파일은 양식 참고용이며 실제 시크릿은 클라우드 UI에서 설정.
# ----------------------------------------------------------------

# 데모 페이지 접근 비밀번호 (영상 제작자에게만 공유)
APP_PASSWORD = "change-me-strong-password-here"
